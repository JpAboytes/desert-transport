# URL base del API Gateway (sin slash final)
VITE_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/prod

# Clave pública VAPID para Web Push (generada con `web-push generate-vapid-keys`)
VITE_VAPID_PUBLIC=BXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
